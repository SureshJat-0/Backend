<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage - BlogApp - Suresh</title>

    <style>
        body {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande';
            overflow-x: hidden;
        }

        .container {
            text-align: center;
        }

        .btns {
            display: flex;
            justify-content: space-between;
            margin: 0.3rem 0.8rem;
            padding: 0.1rem;
        }

        .profile a,
        .postBlogBtn a {
            margin: 20px;
            padding: 5px 20px;
            font-size: 1.5rem;
            font-weight: bolder;
            border: 2px solid black;
            border-radius: 1.5rem;
            text-decoration: none;
        }

        .allBlogs {
            margin: 5px;
            padding: 5px;
        }

        .blog {
            border: 2px solid black;
            border-radius: 1rem;
            text-align: left;
            margin: 1rem;
            padding: 1rem;
        }

        .blog a {
            text-decoration: none;
        }

        .blog a:hover {
            text-decoration: underline;
        }
    </style>

</head>

<body>
    <div class="container">
        <h1>All Blogs</h1>
        <div class="btns">
            <div class="postBlogBtn"><a href="/blog">Post a Blog</a></div>
            <div class="profile"><a href="/profile">Profile</a></div>
        </div>
        <div class="allBlogs">

            <% if (allBlogs.length===0) { %>
                <p>No blogs to display</p>
                <% } %>

                    <% allBlogs.forEach(blog=> { %>
                        <div class="blog">
                            <h3>
                                <%= blog.blogAuthor %>
                            </h3>
                            <p>Blog Title : <%= blog.blogTitle %>
                            </p>
                            <a href="/blog/<%= blog.id %>">Click for detail</a>
                        </div>
                        <% }) %>

        </div>
    </div>
</body>

</html>